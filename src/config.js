const config = {
    //  API_BASE_URL: "http://127.0.0.1:5000", // Change this URL when needed
    
    API_BASE_URL: "http://3.140.209.59:8000", // Change this URL when needed
};

export default config;
